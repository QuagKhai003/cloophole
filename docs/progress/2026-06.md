# Progress — 2026-06

Newest at top. One entry per unit of work. (One file per month: `YYYY-MM.md`.)

---

## 2026-06-22 — auto-watch on by default
Branch `feat/auto-watch`. Tests **26**. No more manual `report` needed.

- **config `poll_enabled` default → true.** The daemon now auto-probes for the limit
  (gentle `claude -p` every `poll_interval_min`, default 30) and arms itself on detect;
  first probe runs on daemon start (catches an already-limited state).
- **menu/README** reworded "idle poll" → "auto-watch" with the quota-cost note.
- **Tests:** +1 (default poll_enabled true). Existing poll tests pass explicit flags.

## 2026-06-22 — terminal menu UI; drop web dashboard + tray (ADR-0006)
Branch `feat/terminal-menu`. Tests **25** green. UI is now a terminal menu; back to
**zero runtime deps**.

- **menu.py** — interactive terminal menu: status header (daemon up?, phase, countdown,
  live session, dir, queue, poll) + numbered actions (fire / queue / report / poll /
  pin dir / clear / refresh / stop daemon / quit). Stdlib only.
- **removed** `ui.py` (http dashboard) + `app.py` (pystray tray) + `pystray`/`Pillow`
  deps + `tkinter` + `ui_enabled`/`ui_port` config + the daemon's UI thread.
- **runner.launch** now starts the background watcher daemon (`pythonw -m cloophole
  daemon` / frozen exe), detached + hidden; CLI is `open` (daemon + menu) / `menu` /
  `close`. Dropped `ui`/`open`-browser/`_app`. PyInstaller spec drops GUI imports.
- **first feature on a branch** per the new branch-per-feature rule.
- **Docs/README/CLAUDE** rewritten for the terminal-menu model. ADR-0006 added.
- **Tests:** removed test_ui; runner test now asserts `daemon` launch; +menu helper.
  Net **26 → 25**. Live-verified menu render + detached daemon lifecycle.

## 2026-06-22 — standalone .exe + one-line PowerShell installer (ADR-0005)
Tests **26** green. Install is now `irm …/install.ps1 | iex` then `cloophole open`
— no Python, no pip.

- **packaging/** — `entry.py` (PyInstaller shim) + `cloophole.spec` → onefile
  `dist/cloophole.exe` (~19 MB, console; tray child uses CREATE_NO_WINDOW). Verified
  status/open/attach/UI/close all work from the exe with no Python present.
- **install.ps1 / uninstall.ps1** — download the exe to `%LOCALAPPDATA%\Programs\
  cloophole`, manage user PATH; no admin. `OWNER/REPO` placeholders.
- **runner._app_command** — relaunches `sys.executable _app` when frozen (no python).
- **cloophole uninstall** — frozen-aware: removes PATH + schedules install-dir delete.
- **.github/workflows/release.yml** — on `v*` tag: test → PyInstaller → attach
  `cloophole.exe` to the GitHub Release the installer pulls from.
- **Docs/README** — irm one-liner is the headline install; pip path kept for devs.

## 2026-06-22 — desktop tray app (ADR-0003); no run-at-logon
Tests 20 → **26**. cloophole is now an installed-feeling app: `cloophole open` →
tray icon; survives terminal close; `cloophole close` / tray Quit to stop.

- **app.py** — pystray system-tray app: dynamic icon color + title (phase + countdown),
  menu (Open dashboard / Fire now / Idle poll toggle / Set queue note via tkinter /
  Quit), native toast on entering FIRING + a start hint. Runs the watcher loop + UI on
  daemon threads; tray on the main thread.
- **runner.py** — `open` = launch-or-attach (`is_running` via pid + `winproc.pid_alive`,
  else spawn `pythonw -m cloophole _app` detached/hidden); `close` = stop the pid tree.
- **daemon.py** — refactored: `claim_pid`/`release_pid`/`loop(cfg, stop)`/`start_ui` so
  the tray and the headless `run()` share one loop; single-instance for both.
- **winproc.pid_alive** — liveness check used by runner + daemon guard.
- **CLI** — `open`/`close`/`uninstall` (+ internal `_app`); dropped logon `install`/
  `start`/`stop`. `install_win.py` demoted to legacy autostart cleanup (uninstall only).
- **deps** — pystray, Pillow added to pyproject (lazy-imported in app.py only).
- **Docs** — ADR-0003 COMPLETE, ROADMAP Phase A + 5.1 superseded, DATA_MODEL app
  lifecycle, CLAUDE/README rewritten for the app flow.
- **Tests:** +6 (runner pid/launch/stop, app image/title). **20 → 26.** Live-verified
  open → attach → close.

## 2026-06-22 — daemon serves the UI; `open` command
Tests 19 → **20**. The UI is now reachable right after install (no separate process).

- **daemon serves the UI** — `ui.start_background(port)` on a daemon thread, started by
  `daemon.run` when `ui_enabled` (new config, default true). Live at
  `http://127.0.0.1:8787`. Verified end-to-end (daemon up → `/state` responds).
- **CLI** — `cloophole open` (browser) + `status` now prints the UI URL.
- **Tests:** +1 UI (ephemeral port 0, serves `/state` + HTML). **19 → 20.**

## 2026-06-22 — one-command no-admin install + single-instance daemon
Tests **19** green. Removes the admin/elevation step from the install flow.

- **`install` is now idempotent + no-admin** — stops any old daemon, best-effort
  drops a leftover Task Scheduler task (quiet if denied), writes the Startup shim,
  starts detached. Re-runnable. README install is now just two commands.
- **single-instance daemon** (`daemon._already_running` + `winproc.pid_alive`) — a
  leftover task + the shim can't double-fire; the second daemon exits.
- **`start_now`/`stop` are liveness-checked** (ignore a stale pid file).

## 2026-06-22 — git init, drop template kit, multi-dir fire
First git commit (repo initialized). Tests 17 → **19**, all green offline.

- **repo** — `git init` + `.gitignore`; first commit of the whole project.
- **removed the Dev Workflow Kit** source (`README.md` kit, `WORKFLOW.md`,
  `templates/`, `prompts/`) per user; promoted `README.cloophole.md` → `README.md`.
  Fixed dangling refs in CLAUDE.md + ONBOARDING.
- **multi-directory fire** (plan §11 decided) — `winproc.detect_all` returns every live
  session's cwd; `daemon._fire_dirs` fires `--continue` in **all** of them, or in a
  pinned `work_dir`. `detect_session`→`detect_sessions` (returns a list). +2 tests.
- **Docs** — DATA_MODEL (fire targets, work_dir = pin), ROADMAP backlog decided, BUGS
  B6 narrowed.
- **Tests:** +2 (all-dirs fire, pin overrides). **17 → 19.**

## 2026-06-22 — Phase 3 idle poll (ADR-0002) + user-feedback fixes
No branch yet (repo not git-initialized). Tests 12 → **17**, all green offline.

- **probe.py** — gentle `claude -p` idle probe; returns `(limited, text)` via
  `subproc.run` (no console window). Public-CLI only (Golden Rule).
- **reset_parser.is_limit_message** — shared limit detector; `fire.still_limited` now
  reuses it so probe + fire can't diverge (was a B3 drift risk).
- **state.last_poll** + **daemon.tick** — WATCHING poll branch arms WAITING when a
  probe reads as limited; cadence gated by `poll_enabled`/`poll_interval_min`.
- **CLI** — `poll on|off`, `start`, `stop`; clearer `report` parse-fail hint.
- **subproc.py** — `CREATE_NO_WINDOW` wrapper. Fixes **B4** (blank "claude" window on
  fire — user-reported).
- **install_win.py** — **B5 fix**: default install is a user Startup-folder `.vbs` shim
  (no admin); Task Scheduler now opt-in via `install --task`; `install` starts the
  daemon immediately (no reboot); `start`/`stop` via pid.
- **Docs** — ADR-0002 (COMPLETE), ROADMAP P3 ✅ + 5.1a, DATA_MODEL (last_poll, probe,
  install methods), BUGS B4/B5 resolved + B6 logged.
- **Tests:** +5 idle-poll (probe stubbed, never networked). **12 → 17.**

## 2026-06-22 — Workflow-kit adoption + source header briefs
No branch yet (repo not git-initialized). Docs only; 12 tests still green.

- **CLAUDE.md + docs/** — instantiated the Dev Workflow Kit for cloophole:
  CONVENTIONS, ROADMAP (phases mapped to plan §10), STATUS, DATA_MODEL, BUGS (B1–B3),
  ONBOARDING, ADR-0001 (retroactive, marks Phases 1/2/4/5.1 complete), decisions index.
- **cloophole/*.py** — added the standard header brief (@context/@done/@todo/@limits/
  @affects) to every module so each file self-describes.
- **Golden Rule fixed:** no Claude Code internals; act only via the public CLI + OS
  process inspection.

## 2026-06-22 — Phase 1+2+4+5.1: Windows-first build (ADR-0001)
Initial implementation, pre-kit. 12 offline tests; verified vs. real `claude.exe`.

- **engine** — `state.py` (state machine), `config.py`, `paths.py`, `daemon.py` loop.
- **reset_parser.py** — clock / relative / ISO limit text → aware UTC. +8 tests.
- **winproc.py** — ctypes Toolhelp detection + PEB cwd read (64-bit). Live-verified.
- **fire.py** — headless `claude --continue`; `still_limited` re-arm.
- **__main__.py** — full CLI. **ui.py** — status page + `/state`. **install_win.py** —
  Task Scheduler. **+4** state-machine tests.
- **Tests:** 0 → **12**.
